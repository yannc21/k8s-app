apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-front.name" . }}-test-connection"
  labels:
    {{- include "my-front.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-front.name" . }}:{{ .Values.services.front.port }}']
  restartPolicy: Never
